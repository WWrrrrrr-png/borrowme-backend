<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.borrowme.helper.HelperMapper">

		<select id="findApprovedRequests" resultType="com.borrowme.helper.dto.HelperRequestDto">
        SELECT id, user_id, title, content, status, created_at
        FROM user_request
        WHERE status = 'APPROVED'
        ORDER BY created_at ASC
    </select>

    <select id="findRequestById" resultType="com.borrowme.helper.dto.HelperRequestDto">
        SELECT id, user_id, title, content, status, created_at
        FROM user_request
        WHERE id = #{id}
    </select>

    <update id="updateRequestStatus">
        UPDATE user_request
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <insert id="insertMatching" parameterType="com.borrowme.helper.dto.HelperMatchingDto"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO matching (request_id, helper_id, status)
        VALUES (#{requestId}, #{helperId}, 'MATCHED')
    </insert>

</mapper>