<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.borrowme.auth.AdminAuthMapper">

    <select id="findByEmail" resultType="com.borrowme.auth.dto.AdminDto">
        SELECT id, email, password, name
        FROM admin 
        WHERE email = #{email}
    </select>  

    <select id="findById" resultType="com.borrowme.auth.dto.AdminDto">
        SELECT id, email, password, name
        FROM admin 
        WHERE id = #{id}
    </select> 
    
    <insert id="insert" parameterType="com.borrowme.auth.dto.AdminDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (email, password, name)
        VALUES (#{email}, #{password}, #{name})
    </insert>

</mapper>